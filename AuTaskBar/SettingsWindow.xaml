<ui:FluentWindow x:Class="AuTaskBar.SettingsWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="clr-namespace:Wpf.Ui.Controls;assembly=Wpf.Ui"
                 Title="Configuracion - FocusBar"
                 Height="520"
                 Width="520"
                 WindowStartupLocation="CenterOwner"
                 WindowStyle="None"
                 ResizeMode="NoResize"
                 Background="{DynamicResource ApplicationBackgroundBrush}"
                 Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                 WindowBackdropType="Mica"
                 WindowCornerPreference="Round"
                 ExtendsContentIntoTitleBar="True"
                 MinWidth="520"
                 MinHeight="520">
    <Window.Resources>
        <SolidColorBrush x:Key="AccentBrush" Color="#589DFF"/>
    </Window.Resources>

    <Grid Margin="16" TextElement.Foreground="{DynamicResource TextFillColorPrimaryBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Configuracion - FocusBar" Margin="0,0,0,8"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <Border Margin="0,0,0,12" Padding="12" CornerRadius="8" BorderThickness="1"
                        BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                        Background="{DynamicResource LayerFillColorDefaultBrush}">
                    <StackPanel>
                        <TextBlock Text="Pomodoro" Foreground="{DynamicResource AccentBrush}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Minutos de Focus:" VerticalAlignment="Center"/>
                            <ui:NumberBox x:Name="FocusMinutesNumberBox" Grid.Row="0" Grid.Column="1" Value="25" MinWidth="120" MaxWidth="160" HorizontalAlignment="Left" Margin="16,0,0,0"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Minutos de Descanso:" VerticalAlignment="Center" Margin="0,8,0,0"/>
                            <ui:NumberBox x:Name="RestMinutesNumberBox" Grid.Row="1" Grid.Column="1" Value="5" MinWidth="120" MaxWidth="160" HorizontalAlignment="Left" Margin="16,8,0,0"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Margin="0,0,0,12" Padding="12" CornerRadius="8" BorderThickness="1"
                        BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                        Background="{DynamicResource LayerFillColorDefaultBrush}">
                    <StackPanel>
                        <TextBlock Text="Barra" Foreground="{DynamicResource AccentBrush}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Ancho (px):" VerticalAlignment="Center"/>
                            <ui:NumberBox x:Name="BarWidthNumberBox" Grid.Row="0" Grid.Column="1" Value="1280" Minimum="700" Maximum="5000" MinWidth="120" MaxWidth="160" HorizontalAlignment="Left" Margin="16,0,0,0"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Alto (px):" VerticalAlignment="Center" Margin="0,8,0,0"/>
                            <ui:NumberBox x:Name="BarHeightNumberBox" Grid.Row="1" Grid.Column="1" Value="90" Minimum="40" Maximum="400" MinWidth="120" MaxWidth="160" HorizontalAlignment="Left" Margin="16,8,0,0"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Opacidad (%):" VerticalAlignment="Center" Margin="0,8,0,0"/>
                            <ui:NumberBox x:Name="BarOpacityNumberBox" Grid.Row="2" Grid.Column="1" Value="70" MinWidth="120" MaxWidth="160" HorizontalAlignment="Left" Margin="16,8,0,0"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Padding="12" CornerRadius="8" BorderThickness="1"
                        BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                        Background="{DynamicResource LayerFillColorDefaultBrush}">
                    <StackPanel>
                        <TextBlock Text="Comportamiento" Foreground="{DynamicResource AccentBrush}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>

                        <ui:ToggleSwitch x:Name="MarqueeToggle" Content="Activar marquesina de textos centrales"/>
                        <ui:ToggleSwitch x:Name="PinnedToggle" Content="Iniciar con barra fijada (Topmost)" Margin="0,8,0,0"/>
                        <ui:ToggleSwitch x:Name="StartupToggle" Content="Ejecutar al iniciar Windows" Margin="0,8,0,0"/>

                        <Grid Margin="0,12,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Tema de la aplicacion:" VerticalAlignment="Center"/>
                            <ComboBox x:Name="AppThemeModeComboBox" Grid.Column="1" SelectedIndex="0" MinWidth="160" HorizontalAlignment="Left" Margin="16,0,0,0">
                                <ComboBoxItem Content="System"/>
                                <ComboBoxItem Content="Dark"/>
                                <ComboBoxItem Content="Light"/>
                            </ComboBox>
                        </Grid>

                        <Grid Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Color principal:" VerticalAlignment="Center"/>
                            <ComboBox x:Name="PrimaryColorComboBox" Grid.Column="1" SelectedIndex="0" MinWidth="160" HorizontalAlignment="Left" Margin="16,0,0,0">
                                <ComboBoxItem Content="Ocean"/>
                                <ComboBoxItem Content="Amethyst"/>
                                <ComboBoxItem Content="Emerald"/>
                                <ComboBoxItem Content="Sunset"/>
                                <ComboBoxItem Content="Rose"/>
                                <ComboBoxItem Content="Amber"/>
                                <ComboBoxItem Content="Violet"/>
                            </ComboBox>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Width="92" Margin="0,0,8,0" IsDefault="True" Click="SaveButton_Click">Guardar</Button>
            <Button Width="92" IsCancel="True">Cancelar</Button>
        </StackPanel>
    </Grid>
</ui:FluentWindow>


